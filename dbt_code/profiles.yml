dbt_code:
  target: dev
  outputs:
    dev:
      aws_profile: default
      type: athena
      s3_staging_dir: s3://iceberg-data-bucket-67/staging/
      s3_data_dir: s3://iceberg-data-bucket-67/data/
      region_name: eu-west-1
      schema: dbt_database
      database: awsdatacatalog
      work_group: primary
